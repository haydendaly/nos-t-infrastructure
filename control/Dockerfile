FROM node

WORKDIR /app
COPY package.json /app
RUN npm install
COPY . /app/

ENV API_HOST=$API_HOST API_PORT=$API_PORT API_WS_PORT=$API_WS_PORT BROKER_HOST=$BROKER_HOST BROKER_PORT=$BROKER_PORT SLEEP=$SLEEP
EXPOSE 8000
CMD ["npm", "start"]